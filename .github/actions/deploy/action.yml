name: 'Hello World'
description: 'Greet someone'
inputs:
  config:
    description: 'Dynamic inputs'
    required: true
    default: ''

runs:
  using: "composite"
  steps:
    - name: Replace .env File
      shell: bash
      run: |
        echo "${{ inputs.config }}" > ./apps/back/api-gateway/.env

    - name: Show .env File
      shell: bash
      run: |
        cat ./apps/back/api-gateway/.env
